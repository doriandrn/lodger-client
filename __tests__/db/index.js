import collections from '../../lodger/db/collections'

describe('DB', () => {
  describe('1. Colectiile', () => {
    test('se instantiaza', () => {
      expect(collections).toBeDefined()
    })
  })
})
