import Store from 'lodger/lib/Store'

describe('Store', () => {
  describe('helpers', () => {
    describe('.')
  })
  describe('new ()', () => {
    describe('positive', () => {
      test('contains all modules', () => {

      })
    })

    describe('negative', () => {

    })
  })
})