.load
  position relative
  z-index 0
  min-height 40px

  &:before
    content ''
    position absolute 0
    z-index -1
    background-image embedurl('~static/loaders/main.svg')
    background-position 50% 50%
    background-repeat no-repeat
    transform scale(.5)
    transition transform .15s ease-out
