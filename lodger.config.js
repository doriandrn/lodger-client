import { version } from './package.json'

const taxonomii = {
  defaults: {
    criteriu: {
      limit: 25
    }
  },
  asociatie: {
    criteriu: {
      limit: 100
    }
  }
}

export {
  version,
  taxonomii
}
